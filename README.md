# Аудио Плеер

Современный веб-аудиоплеер с поддержкой плейлистов, перемешивания и повторения треков.

## 🎵 Функции

- **Воспроизведение аудио**: Play/Pause, переключение треков
- **Плейлист**: Добавление, удаление, отображение треков
- **Прогресс-бар**: Отображение и перемотка трека
- **Громкость**: Регулировка + mute/unmute (клавиша `M`)
- **Перемешивание (Shuffle)**: Алгоритм Fisher-Yates
- **Повтор (Repeat)**: 3 режима: выкл/все/один
- **Горячие клавиши**: 
  - `Пробел` — Play/Pause
  - `←` / `→` — Предыдущий/Следующий трек
  - `↑` / `↓` — Увеличить/Уменьшить громкость
  - `M` — Mute/Unmute
- **LocalStorage**: Сохранение настроек и метаданных
- **Экспорт/Импорт**: JSON-файлы плейлиста
- **Адаптивность**: Mobile-friendly дизайн

## 🚀 Быстрый старт

1. Откройте `index.html` в браузере
2. Загрузите аудиофайлы через кнопку "📁 Добавить треки"
3. Наслаждайтесь музыкой!

## 🔧 Технические детали

### Исправленные проблемы

1. **Утечка памяти**: Добавлена очистка blob URL через `URL.revokeObjectURL()` при удалении треков и ошибках загрузки
2. **Логика shuffle**: Исправлена обработка случая, когда текущий трек не найден в shuffledIndices (добавлена проверка `currentIndex === -1`)
3. **Магические числа**: Все числовые значения вынесены в константы (`VOLUME_STEP`, `DEFAULT_VOLUME`, `MAX_VOLUME`, `RESTART_THRESHOLD`)
4. **Обработка ошибок воспроизведения**: 
   - Добавлена функция `handleAudioError()` для обработки всех типов ошибок HTML5 Audio
   - Автоматический переход к следующему треку при ошибке воспроизведения
   - Визуальные уведомления об ошибках вместо console.error
5. **Состояние громкости**: Добавлено централизованное управление состоянием громкости и mute
6. **Ошибка currentTime**: Добавлена проверка на валидность `audioPlayer.duration` перед установкой `currentTime`
7. **Валидация источников**: Проверка наличия и валидности src у трека перед воспроизведением
8. **Уведомления**: Добавлена функция `showError()` для отображения красивых уведомлений об ошибках
9. **Загрузка файлов**: Улучшена обработка загрузки с подсчетом успешных/неуспешных файлов

### Структура проекта

```
/workspace/
├── index.html        # HTML-разметка
├── styles.css        # Стили CSS
├── script.js         # JavaScript логика
└── README.md         # Этот файл
```

### Рекомендации по дальнейшему улучшению

1. **Визуальная обратная связь**:
   - Добавить индикатор загрузки файлов
   - Заменить `alert()` на красивые уведомления (toast)

2. **Оптимизация производительности**:
   - Debounce для события `timeupdate`
   - Виртуализация длинных плейлистов (>100 треков)

3. **Дополнительные функции**:
   - Поддержка обложек альбомов (ID3 теги)
   - Темная/светлая тема
   - Режим вечеринки (синхронизация нескольких устройств)
   - Эквалайзер

4. **Тестирование**:
   - Добавить unit-тесты для критических функций
   - E2E тесты для основных сценариев использования

## 📄 Лицензия

MIT
