(script.js is a source file, so no additional ignores are needed beyond what might already exist for general JavaScript project artifacts, but since only a JS source file was modified and no build artifacts/directories are present in the changes, no new rules are necessary)

Nothing to output.